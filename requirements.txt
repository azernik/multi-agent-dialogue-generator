openai>=2.8.1
pyyaml>=6.0
python-dotenv>=1.0.0

# HuggingFace model support (optional - only needed for --hf-model)
transformers>=4.44.0
# For CPU-only testing: pip install torch --index-url https://download.pytorch.org/whl/cpu
# For GPU support: pip install torch (or install from pytorch.org with CUDA)
torch>=2.0.0
peft>=0.9.0
# bitsandbytes requires CUDA - only needed for 4-bit quantization (use --no-4bit on CPU)
# bitsandbytes>=0.41.0
accelerate>=0.20.0 