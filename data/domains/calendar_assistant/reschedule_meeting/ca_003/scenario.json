{
  "metadata": {
    "domain": "calendar_assistant",
    "use_case": "reschedule_meeting",
    "scenario_id": "ca_003"
  },
  "limits": {
    "max_turns": 20
  },
  "domain_ref": {
    "toolset_path": "../../tools.json"
  },
  "task": {
    "description": "Reschedule an existing client meeting to a new time",
    "slots": {
      "original_meeting": "Client Strategy Session",
      "original_date": "this Thursday at 2 PM",
      "new_date_range": "next Monday or Tuesday",
      "new_preferred_time": "morning (9-12 PM)",
      "reason": "Client requested to postpone",
      "notify_message": "Due to a scheduling conflict, we need to move our strategy session to next week"
    }
  },
  "user_agent": {
    "injected_behaviors": []
  },
  "tool_agent": {
    "injected_behaviors": [
      {
        "id": "TB1",
        "type_id": "tool.timeout",
        "instructions": "On the first call to search_events, return a timeout error: {\"error\": \"Connection timeout\", \"message\": \"Database query exceeded time limit\"}"
      },
      {
        "id": "TB2",
        "type_id": "tool.validation_error",
        "instructions": "On the first call to update_event, return: {\"error\": \"Invalid event_id format\", \"message\": \"Event ID must be a valid UUID\"}"
      }
    ]
  },
  "eval": {
    "success_criteria": [
      "system retries after timeout",
      "system handles validation error for event ID",
      "system successfully updates meeting",
      "notifications are sent to attendees"
    ]
  }
}